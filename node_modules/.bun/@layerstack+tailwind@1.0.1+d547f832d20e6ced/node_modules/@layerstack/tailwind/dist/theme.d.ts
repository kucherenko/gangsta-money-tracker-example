import { type Color } from 'culori';
export type SupportedColorSpace = 'rgb' | 'hsl' | 'oklch';
export declare const semanticColors: readonly ["primary", "secondary", "accent", "neutral"];
export declare const stateColors: readonly ["info", "success", "warning", "danger"];
export declare const colors: ("accent" | "success" | "neutral" | "danger" | "primary" | "secondary" | "info" | "warning")[];
export declare const shades: number[];
export declare const colorNames: string[];
/**
 * Generate theme colors (ex. { primary: hsl(var(--color-primary) / <alpha-value>), ... })
 */
export declare function createThemeColors(colorSpace: SupportedColorSpace): any;
/**
 * Get themes names split into light and dark collections determined by `color-scheme` property
 */
export declare function getThemeNames(themes: Record<string, any>): {
    light: string[];
    dark: string[];
};
export interface NestedColors {
    [key: string]: string | NestedColors;
}
/**
 * Flatten nested color objects into a single-level color object with concatenated keys
 */
export declare function flattenThemeColors(themeColors: NestedColors, keys?: (string | number)[], memo?: Record<string, string>): any;
/**
 * Convert names to CSS variables and color values common color space (hsl, oklch, etc) and space separated
 */
export declare function processThemeColors(themeColors: NestedColors, colorSpace: SupportedColorSpace): any;
/**
 * Convert color to space separated components string
 */
export declare function colorVariableValue(color: Color | string, colorSpace: SupportedColorSpace, decimals?: number): string | undefined;
/**
 * Process theme to style variables
 */
export declare function themeStylesString(theme: NestedColors, colorSpace: SupportedColorSpace): any;
/** Return a script tag that will set the initial theme from localStorage. This allows setting
 * the theme before anything starts rendering, even when SSR is in use.
 *
 * This feels a bit weird compared to just placing the function directly in svelte:head,
 * but it's the only way to inject the `darkThemes` array into the function.
 **/
export declare function createHeadSnippet(darkThemes: string[]): string;
//# sourceMappingURL=theme.d.ts.map